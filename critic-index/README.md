# critic-index

This subcrate deals with the lower level parts of segmentation, ocr and indexing.

In particular, it provides interfaces to tantivy and python bindings to kraken via pyo3.

## Setup
This crate expects the python packages in `requirements.txt` to be installed locally (in the working environment).
You should do this via a virtual env.

